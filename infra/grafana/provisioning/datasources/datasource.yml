# Auto-provision Prometheus datasource khi Grafana khoi dong.
# Grafana se tu dong connect toi Prometheus — khong can config thu cong.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
